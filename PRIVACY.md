# TextareaVim プライバシーポリシー

最終更新日：2026年8月25日

TextareaVimは、Webページの複数行入力欄をブラウザ内のVim操作で編集できるようにするChrome拡張機能です。

## 取り扱う情報

TextareaVimは、利用者が編集対象として選んだ`textarea`の内容を、編集している間だけブラウザ内のメモリで扱います。
入力内容を保存したり、開発者や第三者のサーバーへ送信したりすることはありません。

TextareaVimは、次の設定をChromeの同期ストレージへ保存します。

- 拡張機能を有効にするかどうか
- 拡張機能を有効または無効にするURLパターン

同期ストレージへ保存した設定は、Chromeの同期機能を有効にしている場合、Googleアカウントに紐づくChrome環境の間で同期されることがあります。
この同期はChromeが提供する仕組みであり、開発者は同期された設定へアクセスできません。

## 情報の利用目的

`textarea`の内容は、Vim操作による編集結果を元の入力欄へ反映するためだけに使います。
保存した設定は、拡張機能を起動するかどうかをページごとに判定するためだけに使います。

TextareaVimは、広告、分析、追跡、利用者のプロファイリングに情報を使いません。

## 情報の共有

TextareaVim自身は、利用者の情報を開発者または第三者へ送信、販売、貸与、共有しません。
ただし、Chromeの同期機能を有効にしている場合、前述した設定はChromeによって同期されます。
外部の分析サービスや広告サービスも利用しません。

TextareaVimが扱う情報は、複数行入力欄をVim操作で編集するという単一の目的にだけ使います。
TextareaVimは、Chrome Web Store User Data Policy（Limited Use要件を含みます）に従って情報を扱います。

## 保存期間と削除

編集対象の`textarea`の内容は永続的に保存しません。
同期ストレージの設定は、利用者が設定を変更または削除するまで、もしくはChrome側で拡張機能のデータを削除するまで残ります。

## 権限

TextareaVimは、設定をChromeの同期ストレージへ保存するために`storage`権限を使います。
Webページの`textarea`を検出して編集機能を提供するため、対応するWebページ上でコンテンツスクリプトを実行します。

## ポリシーの変更

情報の取り扱いを変える場合は、この文書を更新し、必要に応じてChrome Web Storeの掲載情報でも変更内容を案内します。

## 問い合わせ先

不具合やプライバシーに関する問い合わせは、[GitHub Issues](https://github.com/luvpame/textarea-vim/issues)へ送ってください。
