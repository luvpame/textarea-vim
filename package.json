{
  "name": "textarea-vim",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/content.js --bundle --format=iife --platform=browser --target=es2020 --minify --legal-comments=none --outfile=extension/content.js",
    "test": "node --test test/*.test.js",
    "check": "npm run build && npm test && node --check extension/content.js && node scripts/check-bundle.mjs && node -e \"JSON.parse(require('fs').readFileSync('extension/manifest.json', 'utf8'))\""
  },
  "dependencies": {
    "@replit/codemirror-vim": "^6.4.0",
    "codemirror": "^6.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
